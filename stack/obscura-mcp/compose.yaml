services:
  obscura-mcp:
    image: h4ckf0r0day/obscura:0.2.2
    container_name: obscura-mcp
    ports:
      - "${OBSCURA_PORT:-8395}:3000"
    command: "mcp --http --host 0.0.0.0 --port 3000"
    deploy:
      resources:
        limits:
          memory: 2048M
    restart: unless-stopped
